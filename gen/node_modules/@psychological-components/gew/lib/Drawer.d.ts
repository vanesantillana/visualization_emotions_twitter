import { Config } from "./Config";
import { Subject } from "rxjs/Subject";
import "rxjs/add/observable/fromEvent";
export declare class Drawer {
    private mainElement;
    private svg;
    private textSizer;
    private maxTextWidth;
    private gc;
    private config;
    private otherEmotion;
    constructor(config: Config);
    private createPrompt();
    private countLineSize();
    private getRealR();
    private drawAxis();
    private drawLine(x1, y1, x2, y2);
    private drawHeaders();
    private drawBorder();
    private setPosition();
    run(): void;
    elementClick(): any;
    isAllChecked(f: Function): Subject<any>;
}
